version: 0.2
phases:
  build:
    commands:
      - echo "This is master"
      - echo "test"
      - curl ifconfig.me.
# cache:
#   paths:
#     - '/root/.m2/**/*'
#     - '/root/.npm/**/*'
#     - 'build/**/*'